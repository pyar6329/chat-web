{
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-live": "^4.0.2",
    "elm-test": "*"
  },
  "scripts": {
    "start": "elm-live src/Main.elm --open -- --output='dist/main.js' --debug",
    "server": "elm reactor --port=8000",
    "make": "elm make src/Main.elm --debug --output='dist/main.js'"
  }
}
