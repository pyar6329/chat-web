{
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-live": "^4.0.2"
  },
  "scripts": {
    "start": "elm-live src/Main.elm --open -- --output='app.js' --debug",
    "server": "elm reactor --port=8000",
    "make": "elm make src/Main.elm --debug --output='app.js'"
  }
}
